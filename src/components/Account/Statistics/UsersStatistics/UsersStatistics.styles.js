import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  screen: {
    //backgroundColor: "red",
  },
  statisticsType: {
    fontSize: 15,
    fontWeight: "bold",
    textAlign: "center",
    marginTop: 10,
    marginBottom: 10,
  },
});
